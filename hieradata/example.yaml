---
sites:
  vhosts:
    example.drpual.dev:
      server_name: example.drupal.dev
      server_aliases:
        - www.example.drupal.dev
      www_root: /var/www/example.drupal.dev
      listen_port: '80'
      index_files:
        - index.html
        - index.htm
        - index.php
      envvars:
        - 'APP_ENV dev'
  databases:
    example.drupal.dev:
      grant:
        - ALL
      host: localhost
      name: example
      user: example
      password: example
      sql_file: ''
